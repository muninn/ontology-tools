Current Limitations of Document Generator

{} cannot be used except in the context of a replacement string by data


using {_var_} to hold VAR


OWL File Limitations:
Ontology must have 
vann:preferredNamespacePrefix
dc:title
dc:description
owl:versionInfo

NOW using urllib to get subject headings so it should give offline warning and dispose of subject headings and use raw url rather than trying to grab it

